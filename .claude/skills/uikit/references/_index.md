# Component index

Every component → `components/<name>.md`. _(JS)_ = interactive (uk-`<name>` attribute). Use SKILL.md's routing table for common goals; scan here for the long tail.

**Layout** — align (Control the alignment of inline elements depending on the…) · column (Display your content in multiple text columns without having…) · container (This component allows you to align and center your…) · cover_(JS)_ (Expand images, videos or iframes to cover their entire…) · flex (Utilize the power of flexbox to create a wide…) · grid_(JS)_ (Create a fully responsive, fluid and nestable grid layout) · height_(JS)_ (Define the height of elements depending on the viewport…) · margin_(JS)_ (A collection of utility classes to add spacing between…) · padding (A collection of utility classes to add spacing between…) · position (A collection of utility classes to position content) · section (Create horizontal layout sections with different background colors and…) · visibility (Use responsive visibility classes to display or hide elements…) · width (Define the width of elements for different viewport sizes)

**Navigation** — breadcrumb (Create breadcrumbs to show users their location within a…) · dotnav (Create dot navigation to operate slideshows or to scroll…) · dropnav_(JS)_ (Create dropdown menus for any navigation) · iconnav (Create a navigation consisting of icons) · nav_(JS)_ (Defines different styles for list navigations) · navbar_(JS)_ (Create a navigation bar with dropdown menus for the…) · pagination (Easily create a nice looking pagination to navigate through…) · slidenav (Defines navigation with previous and next buttons to flip…) · subnav (Defines different styles for a sub navigation) · tab_(JS)_ (Create a tabbed navigation with different styles) · thumbnav (Create a flexible thumbnail navigation) · totop (Create a simple to-top scroller)

**Media** — image_(JS)_ (Use background images with lazy loading, responsive images and…) · lightbox_(JS)_ (Create a responsive lightbox gallery with images and videos) · parallax_(JS)_ (Animate CSS properties depending on the scroll position of…) · slider_(JS)_ (Create a responsive carousel slider) · slideshow_(JS)_ (Create a responsive slideshow with images and videos) · video_(JS)_ (Display videos as animated images with precise autoplay control,…)

**Forms** — form_(JS)_ (Easily create nice looking forms with different styles and…) · search (Easily create a nice looking search) · upload_(JS)_ (Upload files through a file input form element or…)

**Content** — accordion_(JS)_ (Create a list of items that can be shown…) · alert_(JS)_ (Display success, warning and error messages) · article (Create articles within your page) · badge (Easily create nice looking notification badges) · button (Easily create nice looking buttons, which come in different…) · card (Create layout boxes with different styles) · close (Create a close icon that can be combined with…) · comment (Includes styles for comments, for example for a blog…) · countdown_(JS)_ (Create a simple countdown timer) · description-list (Easily create nice looking description lists, which come in…) · divider (Create dividers to separate content and apply different styles…) · heading (Define different styles for headings) · label (Indicate important notes and highlight parts of your content) · leader_(JS)_ (Create dot leaders for pricing menus or tables of…) · list (Easily create nice looking lists, which come in different…) · marker (Create a marker icon that can be displayed on…) · placeholder (Create a placeholder space that can be used for…) · progress (Defines progress bars that indicate how far along a…) · spinner (Create an animated loading spinner) · table (Easily create nice looking tables, which come in different…) · tile (Create layout boxes with different backgrounds that can be…)

**Utilities** — animation (A collection of smooth animations to use within your…) · background (A collection of utility classes to add different backgrounds…) · base (This component provides the default style for all HTML…) · icon_(JS)_ (Place scalable vector icons anywhere in your content) · inverse_(JS)_ (Inverse colors of any component for light or dark…) · link (Define different styles to integrate links into specific content) · overlay (Create an image overlay, which comes in different styles) · print (Optimize your pages for legible and economic printing) · svg_(JS)_ (Inject inline SVG images into the page markup and…) · text (A collection of utility classes to style text) · transition (Create smooth transitions between two states when hovering an…) · utility (A collection of useful utility classes to style your…)

**Interactive** — drop_(JS)_ (Position any element in relation to another element) · dropbar (Create a toggleable, full-width section called dropbar) · dropdown_(JS)_ (Defines different styles for a toggleable dropdown) · filter_(JS)_ (Filter or sort items in any given layout by…) · modal_(JS)_ (Create modal dialogs with different styles and transitions) · notification_(JS)_ (Create toggleable notifications that fade out automatically) · offcanvas_(JS)_ (Create an off-canvas sidebar that slides in and out…) · scroll_(JS)_ (Scroll smoothly when jumping to different sections on a…) · scrollspy_(JS)_ (Trigger events and animations while scrolling your page) · sortable_(JS)_ (Create sortable grids and lists to rearrange the order…) · sticky_(JS)_ (Make elements remain at the top of the viewport,…) · switcher_(JS)_ (Dynamically transition through different content panes) · toggle_(JS)_ (Hide, switch or change the appearance of different contents…) · tooltip_(JS)_ (Easily create a nice looking tooltip)

